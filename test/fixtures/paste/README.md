# Paste fixtures

Each `name.txt` is text copied from Google Flights. Its `name.json` is what
`parseFlights` should read from it, taking today as 2026-09-24.

To add a case, save the paste as a new `.txt`, write the expected `.json`,
and add the name to `test/parse.test.js`.
